import { defineStyleConfig } from "@chakra-ui/react";

const FormErrorMessage = defineStyleConfig({
  baseStyle: { color: "black", fontWeight: "bold" },
  sizes: {},

  variants: {},
});

export default FormErrorMessage;
